version: '3.8'

services:
  web:
    build: .
    command: daphne -b 0.0.0.0 -p 8000 pastebin.asgi:application
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
